= Налаштування підключення до поштового сервера
include::platform:ROOT:partial$templates/document-attributes/default-set-ua.adoc[]

include::platform:ROOT:partial$admonitions/language-ua.adoc[]

== Загальна інформація

Налаштування каналів зв'язку окремого реєстру реалізовано за допомогою розширення Адміністративного інтерфейсу управління платформою *Control Plane* на етапах створення та редагування реєстру.

Наразі Платформа підтримує одну з наступних опцій налаштувань поштового сервера залежно від вимог реєстру:

* *Платформний поштовий сервер* (`platform-mail-server`)_ -- поштовий сервер, який розповсюджується як внутрішній Платформний сервіс та доступний для використання усіма реєстрами одного екземпляра Платформи.

* *Зовнішній поштовий сервер* (`external-mail-server`)_ -- зовнішній відносно Платформи поштовий сервіс (Gmail тощо).

Налаштування зберігаються у файлі _deploy-templates/values.yaml_ конфігурації реєстру відповідно до прикладів:

.Налаштування з'єднання із зовнішнім сервером
[source, yaml]
----
  global:
    notifications:
      email:
        type: external
        host: smtp.gmail.com
        port: 587
        address: registry@gmail.com
        password: 123
----

.Налаштування з'єднання із внутрішнім сервером
[source, yaml]
----
 global:
   notifications:
     email:
       type: internal
----

== Налаштування SMTP-з'єднання із зовнішнім поштовим сервером

Щоб налаштувати використання зовнішнього поштового сервера для реєстру виконайте наступні кроки.

. Увійдіть до інтерфейсу
include::ROOT:partial$templates/links/platform/administrative/control-plane.adoc[]
.

. Відкрийте меню *Реєстри* та оберіть потрібний реєстр.
+
image:admin:infrastructure/update-registry-components/update-registry-components-1.png[]

. У правому верхньому куті сторінки натисніть *`Редагувати`*.
+
image:admin:registry-management/registry-create/cp-create-registry-ua-4.png[]

. Перейдіть до розділу *Поштовий сервер*.

. З випадного списку оберіть `Зовнішній поштовий сервер`.

. Вкажіть параметри налаштування зовнішнього поштового сервера:

* *Хост*
* *Порт*
* *Поштова адреса*
* *Пароль*

+
image:registry-develop:registry-admin/config-smtp-server/config-smtp-server-03-ua.png[]

. Натисніть `Підтвердити`, щоб зберегти налаштування.

Надалі відправлення всіх повідомлень буде виконуватись з вказаної зовнішньої поштової адреси.

== Налаштування SMTP-з'єднання із Платформним поштовим сервером

Щоб змінити налаштування реєстру на використання внутрішнього поштового сервера, виконайте наступні кроки.

. Увійдіть до інтерфейсу
include::ROOT:partial$templates/links/platform/administrative/control-plane.adoc[]
.

. Відкрийте меню *Реєстри* та оберіть потрібний реєстр.
+
image:admin:infrastructure/update-registry-components/update-registry-components-1.png[]

. У правому верхньому куті сторінки натисніть *`Редагувати`*.

. Перейдіть до розділу *Поштовий сервер*.
+
image:admin:registry-management/registry-create/cp-create-registry-ua-4.png[]

. З випадного списку оберіть `Платформний поштовий сервер`.

. Вкажіть параметри налаштування Платформного поштового сервера:
+
image:registry-develop:registry-admin/config-smtp-server/config-smtp-server-04-ua.png[]

. Натисніть *`Підтвердити`*, щоб зберегти налаштування.
+
[NOTE]
====
Поле `Поштова адреса реєстру` недоступне до редагування, наразі тільки показує шаблон, за яким буде створено ім'я поштової скриньки.
====

+
У результаті формується запит на зміну конфігурації реєстру. Перейдіть до розділу *Реєстри > Запити на оновлення* > ваш останній запит > натисніть іконку перегляду 👁 > відкрийте вікно для підтвердження зміни та натисніть `*Підтвердити*`.

Надалі відправлення всіх повідомлень буде виконуватись зі вказаної внутрішньої (Платформної) поштової адреси:

* `<registry-name>@<registry.platform-domain>`, _де_:

** `<registry-name>` -- назва реєстру;
** `<registry.platform-domain>` -- доменне ім'я кластера.

[NOTE]
====
Налаштування внутрішнього SMTP-сервера виконує адміністратор Платформи. Детальну інформацію можна отримати за посиланням:

* xref:admin:installation/internal-smtp-server-setup.adoc[]
====

